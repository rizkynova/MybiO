<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rizkynova.mybio">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FriendList">

        </activity>
        <activity android:name=".HomeInterest" />
        <activity android:name=".HomeFavFood" />
        <activity android:name=".HomeAbout" />
        <activity android:name=".ClassUtama.Profile" />
        <activity android:name=".ClassUtama.Music" />
        <activity android:name=".ClassUtama.Gallery" />
        <activity android:name=".ClassUtama.DailyActivity" />
        <activity android:name=".ClassUtama.Splashscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ClassUtama.Home" />
        <activity android:name=".ClassUtama.MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>